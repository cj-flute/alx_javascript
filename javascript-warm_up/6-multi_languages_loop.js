#!/usr/bin/node
// A script that prints 3 lines: (like 1-multi_languages.js) but by using an array of stiring and a loop

let string = ['C is fun', 'Python is cool', 'JavaScript is amazing'];

for (let i = 0; i < string.length; i++){
    console.log(string[i]);
}